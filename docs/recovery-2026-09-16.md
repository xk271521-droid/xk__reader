# 2026-09-16 源码恢复记录

## 事件

主对话在未提交工作树上执行了以下 Git 命令：

- `git checkout frontend/src/styles/app.css`
- `git checkout HEAD -- frontend/src/app/App.jsx frontend/src/components/reader/SelectionInsightPanel.jsx`

当前 `HEAD` 指向 2026-05-25 的提交，不能代表 2026-09-16 19:58 的状态。上述命令覆盖了三个文件在 Git 基线之后的未提交内容。

## 已保护状态

- 当前工作区快照：`C:\Users\xk\Desktop\codexwork-recovery-snapshot-20260916-233800`
- 快照包含源码、副本时的 Git 状态和二进制补丁；不包含依赖缓存、Git 元数据和可再生成的构建目录。
- 主对话已收到停止一切写操作的指令。

## 恢复来源

1. `SelectionInsightPanel.jsx`：主对话在覆盖前保存了完整 `git diff HEAD` 输出，可精确应用。
2. 阅读会话布局：2026-08-11 的两个正常桌面 `app.asar` 都保存了 `.reader-session-stack` / `.reader-session` 规则；2026-09-16 的异常构建包缺失该规则。
3. `App.jsx`：2026-09-16 22:24 的桌面构建包保留了覆盖前的运行代码。本任务记录保留了多个源码片段和覆盖前 Git diff；没有 source map，因此不能把压缩构建包直接当作源文件替换。

## 处理原则

- 禁止执行 `git checkout`、`git restore`、`git reset`、`git clean` 或整仓回退。
- 只恢复有明确来源的差异；恢复后先运行前端测试与本地阅读器烟测，再打包桌面端。
- 保留 PDFium 阅读引擎和用户自配 API 的功能目标；不恢复已在此前删除的研究矩阵和摘要模块。

## 2026-09-17 阅读器恢复结果

- 已从覆盖前的原始会话日志精确恢复 `App.jsx` 与 `SelectionInsightPanel.jsx`，没有把 Git 的 2026-05 基线误当作恢复点。
- 已将丢失的阅读会话布局规则放在独立的 `frontend/src/components/reader/readerSessionLayout.css` 并由 `App.jsx` 显式导入，避免共享 `app.css` 再次被回退时折叠阅读器主区域。
- 移除了桌面端误加入的 `window.webContents.openDevTools()`；保留 `.wasm -> application/wasm`，这是 PDFium 正常加载所需的资源类型映射。
- 验证：阅读会话测试 8/8 通过，桌面端测试 44/44 通过；生产构建通过。使用本地真实文献的浏览器烟测确认主阅读区宽 1583px、PDFium 已渲染 3 页、阅读工具栏存在且无浏览器异常。
- 已生成修复后的桌面安装程序：`paper-reader-desktop/release/builder/xk-reader-setup.exe`，并同步同一 SHA-256 副本到 `frontend/public/downloads/xk-reader-setup.exe`。
- 用户自配百度翻译与 AI Key 的新增代码保留在工作区，待阅读器恢复确认后单独修复和验证；不得再通过 Git 回退处理该功能。

## 2026-09-17 工具栏与阅读器排版修复

- 原因确认：`PdfToolbar.jsx` 的工具 ID 并未删除；但正常桌面包中用于“两排工具栏”的 CSS 没有随恢复进入当前源码，导致分类、常用操作和二级工具挤在同一行。选择分类原先还会显示为空白的第二行，因此会误以为工具丢失。
- 修复：新增独立的 `frontend/src/components/reader/readerToolbarLayout.css`，将工具栏固定为上排“分类 / 擦除 / 翻译 / 撤销搜索缩放下载”和下排“具体工具”。样式和组件放在同一目录，避免全局 `app.css` 再次被恢复时影响阅读器。
- 修复：默认“选择”分类的下排重新显示选择、高亮、下划线、评论、手写和截图；批注、图形、插入分类继续显示全部原有工具，未移除 PDFium、划词或全文翻译能力。
- 验证：`PdfToolbar.test.mjs` 4/4 通过，生产构建通过。完整交互冒烟脚本暂无法启动，唯一原因是其硬编码的测试 PDF `STHELAR...pdf` 已不在旧桌面目录；此问题与阅读器代码无关，未对用户文献做任何改动。
